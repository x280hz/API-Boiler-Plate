# API-Boiler-Plate
A clean, reusable ASP.NET Core API template for .NET Core 3.1 with JWT authentication, CRUD support, logging, global exception handling, rate limiting, health checks, and consistent API responses. Built to reduce setup time and provide a solid, maintainable foundation for new API projects.
